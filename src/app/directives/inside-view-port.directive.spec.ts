import { InsideViewPortDirective } from './inside-view-port.directive';

describe('InsideViewPortDirective', () => {
  it('should create an instance', () => {
    const directive = new InsideViewPortDirective();
    expect(directive).toBeTruthy();
  });
});
