import { RainsScrollDirective } from './rains-scroll.directive';

describe('RainsScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new RainsScrollDirective();
    expect(directive).toBeTruthy();
  });
});
