import { SlideInOutDirective } from './slide-in-out.directive';

describe('SlideInOutDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideInOutDirective();
    expect(directive).toBeTruthy();
  });
});
