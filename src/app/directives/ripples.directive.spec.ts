import { RipplesDirective } from './ripples.directive';

describe('RipplesDirective', () => {
  it('should create an instance', () => {
    const directive = new RipplesDirective();
    expect(directive).toBeTruthy();
  });
});
