import { Parallax2Directive } from './parallax-2.directive';

describe('Parallax2Directive', () => {
  it('should create an instance', () => {
    const directive = new Parallax2Directive();
    expect(directive).toBeTruthy();
  });
});
